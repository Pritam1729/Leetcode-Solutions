class Solution {
public:
    char repeatedCharacter(string s) {
        map <char,int> m;
        for(int i = 0;i<s.length();i++) {
            if(m[s[i]] == 1) return s[i];
            m[s[i]]++;
        }
        return 'a';
        
    }
};
